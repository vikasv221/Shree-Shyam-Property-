export interface Plot {
  id: string;
  colony: string;
  village: string;
  area: string;
  price: string;
  description: string;
  photo: string;
  createdAt: number;
}

export interface ContactInfo {
  mobile: string;
  whatsapp: string;
}
